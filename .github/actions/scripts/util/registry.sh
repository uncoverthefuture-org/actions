#!/usr/bin/env bash
# shellcheck shell=bash
# -----------------------------------------------------------------------------
# registry.sh - Helpers for container registry authentication and pulling
# -----------------------------------------------------------------------------
# Purpose:
#   Centralize common registry operations so deploy scripts remain concise and
#   consistent. These helpers are safe no-ops when inputs are missing.
# -----------------------------------------------------------------------------


